localStorage.setItem("voyagoUser", JSON.stringify({
    phone: contact.value.trim(),
    email: contact.value.trim(),
    fullname: fullname.value.trim(),
    username: username.value.trim(),
    password: password.value.trim()
}));
